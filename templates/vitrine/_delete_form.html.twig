<form method="post" action="{{ path('app_vitrine_delete', {'id': vitrine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vitrine.id) }}">
	<button class="btn btn-danger my-2">Delete</button>
</form>
